input_dir: assets/localizations
output_dir: src/assets/localizations

framework: react
class_name: LibLocalizations

base_locale: en
fallback_locale: en
strict: true

# Controls whether the module folder name is prepended to every generated key
# as a namespace prefix.
#
#   "module" (default) → auth.login.page_title
#   "none"             → login.page_title  (use only when all keys are globally unique)
#
namespace_prefix: none

generated:
  runtime_entry_file: lib-localization.ts
  runtime_types_file: lib-localization.types.ts
  runtime_manifest_file: lib-localization.manifest.json

validation:
  max_depth: 10
  require_key_parity: true
  require_placeholder_parity: true
  require_structured_parity: true
  allow_custom_metadata: true
  validate_path_metadata_consistency: true

reporting:
  output_dir: src/assets/localizations/reports
  coverage_format: json

